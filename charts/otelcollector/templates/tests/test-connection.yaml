apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otelcollector.fullname" . }}-test-connection"
  labels:
    {{- include "otelcollector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      {{- /* The health port is targeted when declared, the first service port otherwise. */}}
      args: ['{{ include "otelcollector.fullname" . }}:{{ include "otelcollector.servicePortNumber" (dict "ctx" . "port" "health" "optional" true) }}']
  restartPolicy: Never
